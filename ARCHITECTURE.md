# هيكلية نظام إدارة الديون (debt-manager-saas)

وثيقة مرجعية شاملة تجمع كل قرارات التخطيط المتفَق عليها، منظّمة حسب الموضوع للرجوع إليها عند التنفيذ.

---

## 1) نظرة عامة على المشروع

النظام يأتي بمنتجين:

1. **النسخة الأونلاين (SaaS)** — تطبيق مركزي متعدد المستأجرين، تفصيله بالكامل بالأقسام 3–10 أدناه.
2. **النسخة الأوفلاين** — بيانات العمل محلية على جهاز المستخدم، لكن الاشتراك مُدار مركزياً بنفس نظام الأونلاين. تفصيلها بالقسم 11.

النظام نفسه (النسخة الأونلاين) يتكون من طبقتين فقط:

1. **تطبيق المستخدم (Tenant App)** — الشخص الذي يعطي ديوناً للناس ("المستأجر" بنظام الـSaaS). يدير المدينين والديون والدفعات الخاصة به فقط.
2. **لوحة تحكم مدير المشروع (Platform Dashboard)** — مالك المنصة. يدير المشتركين: الاشتراكات، التفعيل/التعطيل، الباقات.

**لا طبقة ثالثة**: المدين (الشخص الآخذ الدين) ليس له حساب دخول إطلاقاً — سجل بيانات فقط داخل تطبيق المستخدم (اسم، هاتف، عنوان، ملاحظة). التواصل معه يتم من هاتف المستخدم شخصياً عبر روابط مباشرة (تفصيل بالقسم 8.4)، بلا بوابة أو خادم وسيط.

**البصمة البصرية موحّدة تماماً** بين تطبيق المستخدم ولوحة مدير المشروع — نفس هوية التصميم (Glassmorphism/Neon، خط Cairo، مكوّنات مخصّصة لا عناصر نظام التشغيل)، فرق بالأقسام فقط لا بالشكل.

---

## 2) المعايير التقنية العامة

*(معايير عامة تُعتمد لأي مشروع من هذا النوع، انظر أيضاً ملف المعايير الأصلي المستقل)*

### المعمارية الأساسية
- Laravel (PHP 8.2+) حصراً — الخادم هو العقل: المنطق والتحقق والصلاحيات فيه دائماً.
- Vue 3 عبر Inertia.js للواجهات التفاعلية، Blade للصفحات البسيطة (دخول، صفحات عامة، وصولات مطبوعة).
- Vite + Node على جهاز التطوير فقط — `npm run build` محلياً يرفع الناتج إلى `public_html/build/`. السيرفر لا يحتاج Node (توافق cPanel).
- SaaS متعدد الزبائن: قاعدة واحدة مشتركة، عزل صفوف عبر `tenant_id` في كل جدول (نطاق عام إجباري يفلتر كل استعلام تلقائياً) + باقات وحدود وتجربة مجانية.
- MySQL/MariaDB: `ENGINE=InnoDB` صراحة في كل هجرة، `softDeletes` في كل جدول، المبالغ أعداد صحيحة بلا كسور.
- استضافة cPanel/CloudLinux + LiteSpeed. تطبيق Laravel خارج جذر الويب في `~/app`، الدخول حصراً من `~/public_html/index.php`.
- Service Worker موحد `sw.js` في جذر `~/public_html/`.
- RTL أساسي + ثلاث لغات (عربي/إنكليزي/كوردي) من ملفات ترجمة.
- **عزل BiDi**: كل تاريخ/رقم/رابط داخل صفحة عربية يُعزَل بـ`<bdi>` أو `direction:ltr` (يشمل حقول الإدخال والنصوص المعروضة).
- **صيغة التاريخ الموحّدة بكل النظام**: كل تاريخ مُعروض بأي مكان بترتيب من اليسار إلى اليمين **يوم ← شهر ← سنة**، والشهر رقم لا اسم (مثال: `23 08 2026`). اسم اليوم بالكلمات (مثل "السبت") يظهر بجانب التاريخ الرقمي لا مدمَجاً به، ونص عادي بلا عزل BiDi (العزل للجزء الرقمي فقط).
- **مرجع التصميم البصري المُعتمَد (نموذج FINNOVA)**: داشبورد مالي حدّد الهوية الفعلية —
  - **الألوان**: بنفسجي/إنديغو (تدرّج فاتح لأزرق بنفسجي) كلون أساسي وحيد للعناصر التفاعلية (أزرار، تبويب نشط، رسوم بيانية). خلفية بيضاء/رمادي فاتح جداً؛ لوحات تباين داكنة (كحلي/رمادي غامق) للأقسام المميَّزة. نص رمادي مكتوم للثانوي، عناوين/أرقام داكنة عريضة.
  - **البطاقات**: بيضاء، زوايا دائرية كبيرة (~16–20px)، ظل ناعم خفيف، حشوة سخية. بطاقات المؤشرات: أيقونة برأس دائري بخلفية ملوّنة خفيفة + رقم كبير + مؤشر تغيّر (سهم/نسبة).
  - **عناصر دائرية بالكامل (Pill-shaped)**: تبويبات، أزرار الفلترة، الشارات، أزرار الإجراء الأساسية — `border-radius: 9999px` دائماً.
  - **الحالة النشطة**: تعبئة بنفسجية كاملة + نص أبيض، تبرز عن العناصر الخاملة.
  - أفاتار دائري دائماً؛ رسوم بيانية بتدرّج بنفسجي وخطوط شبكة خفيفة/معدومة؛ شارات حالة صغيرة بشكل حبة دواء بخلفية لونية خفيفة.
  - **الوضع الداكن**: نفس البنفسجي أساسياً، الخلفيات البيضاء تعكس لدرجات داكنة والنصوص تنعكس معها.
  - هذا المرجع هو التطبيق الفعلي لخط "Glassmorphism + Neon glowing lines" الأصلي، ويُعتمَد حرفياً عند بناء كل مكوّنات الواجهة.
  - **نطاق محدود بأربع قيود**: (1) رموز تصميم فقط لا بنية صفحة — قرار "الرئيسية بلا رسوم بيانية" بهذا المشروع يبقى نافذاً حرفياً؛ (2) الصورة LTR، كل عنصر اتجاهي يُبنى بمنطق RTL معكوس؛ (3) سقف الكثافة المتفَق عليه بهذا المشروع (عدد بطاقات/أعمدة) يبقى كما هو؛ (4) عناصر لا تنطبق على مجال إدارة الديون (كشرائح بطاقات الدفع) تُستثنى كلياً.
- خط Cairo (مضمَّن محلياً بالنظام)، وضع ليلي/نهاري بمتغيرات CSS، كلاسات مشتركة موحدة.
- كل الأصول مستضافة محلياً (خطوط، أيقونات، مكتبات، حزمة Vue) — ممنوع أي CDN.
- الأداء: OPcache + تنظيف الكاش عند كل نشر (`php artisan optimize:clear` وحذف `storage/framework/views/*.php` وإعادة بناء config/route cache).

### طبقة Vue
- مكونات Vue 3 بصيغة `<script setup>` — مكون لكل شاشة في `resources/js/Pages/`، مكونات مشتركة في `resources/js/Components/`.
- تنقل عبر Inertia بدل `view()`، بيانات عبر `Inertia::render('Page', [...])`.
- نماذج عبر `useForm` من Inertia (حالة تحميل، أخطاء تحت كل حقل).
- الخادم يبقى القفل الحقيقي: تحقق مزدوج دائماً (Vue للتجربة، Laravel للفرض).
- بحث وترقيم من الخادم عبر طلبات Inertia الجزئية — لا جداول ضخمة في المتصفح.
- SweetAlert2 محلياً أو مكون نافذة موحد خاص.
- **لا اعتماد على عناصر واجهة نظام التشغيل أو المتصفح الأصلية إطلاقاً**: القوائم المنسدلة، رسائل التحذير/التأكيد، السلايدات، والأيقونات — كلها مكوّنات مخصّصة بالنظام نفسه، لا `<select>` أو `alert()`/`confirm()` أو عناصر Windows/الهاتف الافتراضية. **قالب/نموذج مرجعي للحداثة سيُزوَّد لاحقاً من المستخدم** ليُعتمد عند التنفيذ.

### تحسينات وميزات معززة
- ضغط الصور بـ Intervention Image → WebP/JPEG (~72%).
- حقول وسوم (Chips) بإدخال Enter.
- بحث/فلترة/ترقيم من الخادم دائماً.
- مكوّن موحد لحقول المبالغ (فواصل أثناء الكتابة، أرقام إنكليزية، تجريد قبل الإرسال).
- سلة محذوفات (استرجاع/حذف نهائي/حذف الجميع) لكل الأقسام.
- سجل حركات (Activity Log) للعمليات المهمة.
- قفل الأزرار عند الحفظ + منع تكرار العملية في الخادم.
- مكوّنات موحدة للتاريخ/الأرقام بدل حقول المتصفح الأصلية (سلوك موحّد بين الأجهزة، إخفاء أسهم الأرقام على اللمس).
- **حقول أرقام الهاتف**: أرقام فقط — لا حروف ولا رموز، ويجب أن تكون 11 رقماً بالضبط. يُفرض بطبقتين: منع الكتابة بالواجهة + `regex:/^[0-9]{11}$/` بالخادم.
- طباعة تُبنى صراحة بـ`@media print` كاملة (لا الاكتفاء بإخفاء الأزرار) — تتجنب تسرّب قواعد `max-width` الخاصة بالهاتف للطباعة.

### PWA
- منفستان منفصلان: `manifest.webmanifest` (`/`) و`manifest-admin.webmanifest` (`/admin`).
- Service Worker موحد: Cache-First للثابتات، Network-First لـ`/admin` `/login` `/backend`، صفحة `offline.html` مخصصة، لا اعتراض لأي طلب خارجي.
- تثبيت ذكي (نقرة واحدة Chromium، تعليمات مصورة iOS)، زر تثبيت بصفحة الدخول، Standalone Mode.
- Web Push ذاتي بلا طرف ثالث: مفاتيح VAPID خارج المستودع (النشر يفشل صراحة إن غابت). منع تكرار بمفتاح فريد، حذف نقاط ميتة عند 404/410، سقف إرسال، توزيع من cron.

### قواعد الحجم (من اليوم الأول)
- لا استعلامات/صفوف ضخمة داخل قالب مشترك — التجميع والقصّ بالقاعدة.
- تفادي حدّ المتغيرات 65,535 بـ`whereIn` الضخم.
- تفادي `whereDate`/`whereMonth`/`whereYear` (تعطّل الفهارس) — مقارنة مباشرة أو `BETWEEN`.
- حذف ضخم على دفعات (~20 ألف)، الأبناء قبل الآباء، ثم `OPTIMIZE TABLE`.
- فهارس مركّبة على أعمدة تُفلتر معاً (tenant + تاريخ، tenant + حالة).
- اختبار تحميل بأحجام واقعية قبل التسليم.

### النشر والأمان
- نشر بأمر واحد: `npm run build` محلياً → GitHub → السيرفر SSH/SCP بمفتاح → `php artisan migrate --force` يدوياً.
- كسر كاش تلقائي (أسماء ملفات مبصومة من Vite + `?v={{ @filemtime(...) }}`).
- نسخ احتياطية تلقائية قبل كل نشرة + نسخة مسماة قبل كل ميزة كبيرة + يومية بـcron (احتفاظ 30 يوماً)، مع التحقق من صحة النسخة وتجربة الاستعادة.
- حقول كلمة المرور بـ`autocomplete` صريح، تغيير الكلمة يتطلب تأكيداً صريحاً بالخادم.
- الأسرار خارج Git، كلمة مرور القاعدة لا تمر بسطر الأوامر.
- تحقق مزدوج دائماً برسائل عربية، وترجمة كاملة لرسائل التحقق وأسماء الحقول.
- تصحيح لا رفض بالحقول المساعدة — الرفض فقط للحقول التي تفسد البيانات/المال.
- كل عملية مركبة داخل `DB::transaction`.

---

## 3) نموذج البيانات الكامل

```
tenants        — حساب المشترك:
                 name, phone (UNIQUE — يمنع نفس الرقم من أخذ تجربة مجانية ثانية),
                 logo (شعار المكتب، مضغوط),
                 plan_id (لا يبقى فارغاً أبداً — كل حساب جديد يُنسَب لباقة "تجربة مجانية" افتراضية تلقائياً),
                 trial_ends_at, subscription_ends_at,
                 status (active/trial/expired/suspended/cancelled),
                 type (online/offline — يخدم مشتركي الأونلاين والأوفلاين معاً بنفس الجدول),
                 locale (ar/en/ku), theme (light/dark), rows_per_page,
                 due_reminder_days, overdue_grace_days, chronic_overdue_extra_days,
                 debt_receipt_start_number, next_debt_receipt_number,
                 payment_receipt_start_number, next_payment_receipt_number,
                 backup_frequency (daily/weekly/monthly/none)

plans          — الباقات: name, price, duration_days,
                 max_debtors (يُفرَض فعلياً بالخادم),
                 max_devices (افتراضي 1)
                 — يتضمن دائماً صفاً افتراضياً لباقة "تجربة مجانية"، غير قابل للحذف إطلاقاً

devices        — الأجهزة المسجَّلة دخولها حالياً: tenant_id, device_token, last_active_at
                 (يُحذَف السجل فوراً عند تسجيل الخروج؛ تنظيف تلقائي دوري لأي جهاز خامل >30 يوماً)

users          — تسجيل الدخول: tenant_id (null لمدير المشروع), name, phone, password,
                 role (owner/platform_admin — يُفرض من الخادم دائماً، platform_admin عبر أمر Artisan يدوي فقط)

debtors        — العملاء (المديونون له): tenant_id, name,
                 phone (11 رقم، UNIQUE مع tenant_id),
                 address, id_document_image (خاص، غير عام),
                 is_favorite (boolean), note, soft delete

guarantors     — الكفلاء: tenant_id, name, phone (11 رقم، UNIQUE مع tenant_id),
                 address, id_document_image (خاص), note, soft delete

debts          — الدين: tenant_id, debtor_id, amount (int), currency (IQD/USD),
                 description, due_date, payment_type (lump_sum/installments),
                 receipt_number (يُخصَّص عند إنشاء الدين مباشرة),
                 paid_amount (Observer تلقائي، غير قابل للتعديل اليدوي)

debt_installments — الأقساط (لو payment_type=installments): tenant_id, debt_id, seq_number,
                 amount, due_date, paid_amount (Observer تلقائي)

debt_guarantor — pivot many-to-many: debt_id, guarantor_id
                 (كفيل واحد قد يكفل عدة ديون، والدين قد يكون له أكثر من كفيل؛ اختياري دائماً)

payments       — الدفعات: tenant_id, debt_id, debtor_id, installment_id (nullable),
                 amount, paid_at, note, receipt_number (يُخصَّص عند تسجيل الدفعة مباشرة)

creditors      — الدائنون (الاتجاه المعاكس): tenant_id, name,
                 phone (11 رقم، UNIQUE مع tenant_id), address, note, soft delete
                 (بدون صورة مستمسك ولا كفيل)

my_debts       — ديوني (دين على المستخدم نفسه): tenant_id, creditor_id, amount, currency,
                 description, due_date, payment_type, paid_amount (Observer تلقائي)

my_debt_installments — أقساط ديوني (نفس منطق debt_installments)
my_debt_payments      — دفعات المستخدم لدائنيه: tenant_id, my_debt_id, creditor_id,
                        installment_id (nullable), amount, paid_at, note

activation_codes — أكواد التفعيل: code (UNIQUE), plan_id,
                 assigned_tenant_id (مخصَّص لمشترك واحد فقط، لا يتغيّر),
                 expires_at, created_by (platform_admin),
                 redeemed_by_tenant_id, redeemed_at,
                 status (unused/used/expired/cancelled)

plan_requests  — طلب خطة من المشترك: tenant_id, plan_id, requested_at,
                 status (pending/approved/rejected), reviewed_by, reviewed_at, note

backups        — نسخ احتياطية خاصة بكل مشترك (أونلاين): tenant_id, file_path (خاص),
                 size, type (manual/automatic), created_at, soft delete

notifications  — سجل إشعارات داخل التطبيق: tenant_id, type, data (json),
                 read_at (nullable), created_at
                 (منفصل عن push_subscriptions الذي يخص اشتراك المتصفح بالـPush فقط)

activity_logs, push_subscriptions — حسب المعايير العامة
platform_settings — إعدادات مركزية عامة (key/value): trial_days, logo, country,
                 محتوى تبويب "حول" (شرح التطبيق، واتساب التواصل، البريد، اسم الشركة)
```

**لا جدول لتتبّع رسائل واتساب/SMS** — التواصل مع المدين/الكفيل عبر روابط مباشرة من جهاز المستخدم، لا يمر بالخادم إطلاقاً.

**guard واحد (`web`)** يكفي — التمييز بين مستخدم عادي ومدير المنصة عبر عمود `role`.

**منع تكرار المدين/الكفيل**: الهاتف هو مفتاح التطابق الحقيقي لا الاسم. قيد `UNIQUE(tenant_id, phone)` + تحقق فوري (AJAX) عند إدخال رقم موجود مسبقاً، مع خيار الربط المباشر بدل إنشاء سجل مكرر.

**صور المستمسكات**: تُخزَّن على disk خاص (`storage/app/private`) لا `public/storage` — تُعرض فقط عبر راوت محمي بـ`auth` وتحقق ملكية التينانت، برابط موقّع مؤقت الصلاحية (`URL::temporarySignedRoute`).

---

## 4) الراوتات

```
/                تسجيل دخول + صفحة تسويقية بسيطة
/app/*           تطبيق المستخدم (owner) — المدينون، الديون، الدفعات
/platform/*      لوحة مدير المشروع — الاشتراكات، التفعيل، الباقات (role=platform_admin فقط)
```

---

## 5) التسجيل والتجربة المجانية

- تسجيل ذاتي بالكامل (هاتف + كلمة مرور) بلا أي تدخل يدوي من مدير المشروع.
- `trial_ends_at` يُحسب تلقائياً عند التسجيل، وتصبح الحالة `trial` فوراً.
- كل حساب جديد يُنسَب تلقائياً لباقة "تجربة مجانية" افتراضية (بحدود `max_debtors`/`max_devices` معقولة) — `plan_id` لا يبقى فارغاً أبداً.
- مدة التجربة (`trial_days`) إعداد عام بـ`platform_settings` يُعدَّله مدير المشروع — يُطبَّق على الحسابات الجديدة فقط.
- بعد انتهاء `trial_ends_at` بلا اشتراك مفعّل، تتحوّل الحالة تلقائياً لـ`expired`، ويحتاج المشترك لتفعيل حسابه بنفسه عبر كود تفعيل.
- **يمكن طلب/تفعيل باقة مدفوعة حتى أثناء التجربة نفسها** — لا داعي لانتظار انتهائها؛ الموافقة تحوّل الحالة فوراً لـ`active`.
- **منع إساءة الاستخدام**: قيد `UNIQUE` على هاتف `tenants` + `throttle` على راوت التسجيل (حد أقصى تسجيلات لكل IP باليوم). تحقق OTP الكامل مؤجَّل لمرحلة لاحقة.

---

## 6) تطبيق المستخدم — التخطيط العام

### الهيدر (ثابت بكل الصفحات بلا استثناء)
أربعة عناصر:
- **اليمين**: شعار المكتب.
- **اليسار**: اسم المستخدم (يفتح تبويب "إدارة الحساب" مباشرة) + أيقونة جرس الإشعارات بجانبه.
- **الوسط**: اسم اليوم بالكلمات بجانب التاريخ الرقمي (صيغة يوم-شهر-سنة الموحّدة).

### تخطيط الحاسوب
- قائمة جانبية ثابتة تضم كل الأقسام.
- زر تبديل المظهر (فاتح/قاتم) أعلى القائمة الجانبية — تبديل سريع يشارك نفس `tenants.theme`.

### تخطيط الهاتف
فوتر ثابت أسفل الشاشة بأربعة أزرار:
1. **الرئيسية** — ينقل للصفحة الرئيسية مباشرة.
2. **القائمة** — تنسدل قائمة الأقسام من الأسفل (Bottom Sheet)؛ زر تبديل المظهر يوضع أعلى هذه القائمة.
3. **إضافة دين جديد** — يفتح نموذج الدين الموحّد مباشرة بلا خطوة وسيطة.
4. **تسديد** — يفتح أولاً بحث اختيار العميل/الدين، ثم إدخال مبلغ الدفعة.

### قواعد عامة على كل الجداول والشاشات
- رؤوس الجداول ثابتة (Sticky) أثناء التمرير.
- حفظ مكان التصفّح (رقم الصفحة + البحث + الفلتر) عند الرجوع من أي بطاقة تفصيلية.
- زر "رجوع" صريح بكل صفحة فرعية (بطاقة، نموذج، تقرير).
- عند تجاوز `rows_per_page`، النتائج تُقسَّم لصفحات مرقَّمة تقليدية — لا تمرير لا نهائي.

### مركز الإشعارات
- أيقونة جرس بالهيدر ببشارة عددية للإشعارات غير المقروءة.
- الضغط عليها يفتح قائمة/صفحة الإشعارات، الأحدث أولاً؛ كل إشعار ينقل لمكانه المرتبط ويُعلَّم كمقروء تلقائياً.
- جدول `notifications` مستقل عن `push_subscriptions`.
- أنواع الإشعارات: اقتراب استحقاق دين، اقتراب انتهاء التجربة/الاشتراك، تأكيد دفعة جديدة، تأكيد دين جديد، تحوّل لحالة متأخر/متلكئ، نتيجة طلب خطة، نجاح استخدام كود تفعيل.
- كل إشعار داخلي يُرسَل أيضاً كـPush.

### الأمان
- **قفل التطبيق ببصمة/Face ID/رقم سري**: طبقة حماية محلية إضافية (منفصلة عن تسجيل الدخول) تُطلَب عند إعادة فتح التطبيق بعد بقائه بالخلفية لفترة قصيرة — WebAuthn على الويب، رقم PIN كبديل احتياطي دائم.
- **حد الأجهزة المتزامنة**: جهاز واحد فقط افتراضياً (`plans.max_devices=1`) — محاولة دخول من جهاز ثانٍ تُرفَض ما لم تسمح الباقة بأكثر (خدمة مدفوعة). كل خروج يحذف سجل الجهاز من `devices` فوراً.
- معالجة الحالات الاستثنائية: تنظيف دوري (cron) لأي جهاز خامل >30 يوماً (شبكة أمان لانقطاع مفاجئ)، وزر **"تسجيل خروج من كل الأجهزة الأخرى"** بتأكيد كلمة مرور (لحالة الجهاز المفقود).
- **سلوك تسجيل الخروج**: خروج حقيقي فعلاً على الخادم، لكن رقم الهاتف فقط يبقى محفوظاً محلياً (localStorage) ليُعبَّأ تلقائياً بالدخول التالي — كلمة المرور لا تُحفَظ أبداً، تُطلَب دائماً. خيار "تسجيل دخول بحساب آخر" يمسح الرقم المحفوظ.

---

## 7) أقسام تطبيق المستخدم

القائمة الجانبية تتكون من:

### 0) الصفحة الرئيسية
لوحة مؤشرات (بطاقات بلا رسوم بيانية)، تعرض الاتجاهين معاً (ديون له/عليه):
- بطاقات: الديون له، الديون عليه، عدد العملاء، المتأخرات، قريب الاستحقاق، **استحقاقات اليوم بالضبط**.
- كل بطاقة تنتقل لقسمها بفلتر مطابق مُطبَّق تلقائياً (مثال: "المتأخرات" → قسم الديون بفلتر الحالة=متأخر).
- **تقرير فترة زمنية شامل** (منفصل عن كشف حساب شخص واحد): مدى تاريخي مختار، إجمالي الديون الجديدة والمُحصَّل والصافي بكل عملة، قابل للطباعة.

### 1) العملاء (`debtors`)
- بحث محلي (اسم/هاتف) + فلتر (الكل/مفضلة).
- زر "إضافة عميل جديد" مباشرة (بلا دين).
- **إنفاذ حد الباقة**: يُمنع الحفظ بالخادم فعلياً عند بلوغ `max_debtors`.
- أعمدة الجدول (بسيطة عمداً): مفضلة، الاسم، الهاتف (تفاعلي)، إجمالي الدين المتبقي بكل عملة.
- سلة محذوفات.

### 2) الكفلاء (`guarantors`)
نفس تصميم العملاء، بأعمدة مُكيَّفة (لا مفضلة ولا دين): الاسم، الهاتف، **عدد العملاء المتكفَّل بهم حالياً**.

### 3) الديون (`debts` + `debt_installments`)
قائمة كل الديون، إنشاء دين جديد، تسجيل الدفعات. تفصيل النموذج بالقسم 8.

### المتأخرة والمتلكئة (قسمان تلقائيان بعد "الديون")
- **المتأخرة**: `تاريخ الاستحقاق + overdue_grace_days < اليوم`.
- **المتلكئة**: `... + chronic_overdue_extra_days < اليوم` (تأخر مزمن) — يظهر بالقسمين معاً.
- بالأقساط، الحساب لكل قسط على حدة من تاريخ استحقاقه الخاص (لا من تاريخ الدين الأب).
- **تمييز لوني بالجدول**: اسم العميل أصفر (متأخر) أو أحمر (متلكئ، يطغى على الأصفر)، ديناميكي دائماً — يعود للون الطبيعي فور التسديد أو الخروج من الشرط.

### 4) ديوني (`creditors` + `my_debts` + `my_debt_installments`) — الاتجاه المعاكس
ديون على المستخدم نفسه تجاه أشخاص/شركات. نفس منطق "الديون" تماماً (نفس النموذج، حاسبة الأقساط، قاعدة العملات) لكن بجداول منفصلة تماماً حتى لا يختلط "دين له" بـ"دين عليه". الدائن بلا صورة مستمسك ولا كفيل.

### المفضلة
قسم إضافي، خاص بالعملاء فقط — يعرض من وُضعت عليه علامة "مفضلة" (`is_favorite`). لا ينطبق على الكفلاء.

### 5) الإعدادات
تخطيط تبويبات عمودية، ستة تبويبات (تفصيل كامل بالقسم 9).

---

## 8) نموذج إضافة دين جديد

النموذج واحد، وكل طرف (العميل والكفيل) له شريط بحث موحّد واحد **بدون مفتاح تبديل يدوي**:
- يكتب المستخدم الاسم أو الهاتف؛ إن ظهر تطابق يختاره مباشرة؛ وإلا تنفتح تلقائياً حقول شخص جديد كاملة (بنفس المكان).
- تُغطى الحالات الأربع (عميل/كفيل: موجود أو جديد) بنفس النموذج. الكفيل اختياري دائماً — تركه فارغاً يُظهر تنبيهاً تحذيرياً غير مانع فقط.
- إدخال المبلغ عبر Money Input الموحّد.

### 8.1) نوع سداد الدين
- **دفعة واحدة بتاريخ محدد (lump_sum)**.
- **أقساط (installments) — عبر "حاسبة الأقساط الذكية"**: الفترة بين الأقساط وتاريخ أول قسط مطلوبان دائماً؛ طريقتان لتحديد التقسيط (يحسب النظام الطرف الناقص):
  1. **بعدد الأقساط** → يُحسب مبلغ القسط (قسمة صحيحة، الباقي للأخير).
  2. **بمبلغ القسط** → يُحسب عدد الأقساط (تقريب للأعلى)، والأخير يحمل الباقي.
  - **قاعدة صارمة**: كل قسط عدد صحيح بلا كسور دائماً (`intdiv`)، القسط الأخير فقط يمتص الفارق.

### 8.2) تعدد العملات: دينار عراقي ودولار
- حقل `currency` على كل دين — دين واحد بعملة واحدة فقط، لا مزج.
- **قاعدة "بلا كسور" حسب العملة**: الدينار مضاعفات الألف (1000، 5000...)، الدولار مضاعفات الخمسين (50، 100...).
- تُفرض بطبقتين (Money Input + تحقق خادم `multiple_of`). الاستثناء الوحيد: القسط الأخير قد لا يطابق المضاعف.
- **لا تحويل عملة إطلاقاً** — اللوحة الرئيسية والتقارير تعرض إجماليين منفصلين دائماً، لا دمج.

### 8.3) الحفظ
داخل `DB::transaction` واحدة — أي طرف جديد يُنشأ بجدوله، أي طرف موجود يُربط بمعرّفه، الدين يُنشأ مرتبطاً بالعميل والكفلاء عبر `debt_guarantor`. فشل أي جزء يُلغي العملية بالكامل.

### 8.4) التواصل مع أي رقم هاتف — روابط مباشرة (لا بوابة خارجية)
كل رقم هاتف بالنظام عنصر تفاعلي موحّد، الضغط عليه يفتح 4 خيارات:
1. اتصال عادي (`tel:`)
2. اتصال واتساب (`wa.me/<دولي>`)
3. رسالة نصية (`sms:`)
4. رسالة واتساب (`wa.me` بنص افتراضي معبَّأ، قابل للتعديل)

كل الإرسال من جهاز المستخدم مباشرة — لا خادم، لا API، لا تكلفة، لا خطر حظر رقم مشترك. كود الدولة يُقرَأ من إعداد **البلد** المركزي (`platform_settings.country`) لا مُرمَّزاً بالكود. النظام لا يعرف ولا يسجّل نجاح/فشل الإرسال (خارج نطاقه).

### 8.5) الوصولات القابلة للطباعة
**وصل الدين — مستمسك قانوني**: بعد حفظ أي دين، خيار طباعة وصل يضم: ترويسة (شعار + اسم/هاتف الدائن + رقم الوصل التسلسلي)، بيانات الدين والعميل والكفيل، وفي النهاية **اسم العميل والكفيل مع مساحة توقيع يدوي**. رقم الوصل يُخصَّص عند **إنشاء الدين مباشرة** (لا عند الطباعة، حفاظاً على الترتيب الزمني الصحيح)، برقم بداية يُحدَّد مرة واحدة فقط عند الإعداد الأولي ويُقفَل بعدها.

**وصل تسديد الدفعة/القسط**: بعد تسجيل أي دفعة، وصل منفصل بترويسة مشابهة + مبلغ الدفعة والمتبقي — بلا توقيع إجباري (يوثّق عملية تمت فعلاً لا التزاماً مستقبلياً). رقمه التسلسلي منفصل تماماً عن تسلسل وصل الدين.

كلا الوصلين يتبعان معيار الطباعة الصريحة (`@media print` كامل).

---

## 9) بطاقة الشخص التفصيلية وقيود الحذف

### بطاقة العميل
- بياناته الكاملة، الكفلاء المرتبطون به (عبر كل ديونه)، كل ديونه بحالتها.
- إجراءات: تعديل، حذف (ناعم)، تسجيل دين جديد له، تسديد دفعة، تقرير/كشف حساب قابل للطباعة **وللمشاركة** (نص واتساب أو صورة فعلية عبر Web Share API)، تبديل المفضلة، رقمه تفاعلي.

### بطاقة الكفيل
الكفيل نفسه ليس عليه دين — بطاقته تعرض **العملاء الذين يتكفّل بهم** وتحت كل عميل دينه المكفول تحديداً. إجراءات: تعديل، حذف، تقرير.

### قيود الحذف
- **لا حذف عميل عليه دين قائم** (`paid_amount < amount` بأي من ديونه) — الديون المسدَّدة بالكامل فقط لا تمنع الحذف.
- **لا حذف كفيل يتكفّل حالياً بعميل عليه دين قائم**.
- تحقق بطبقتين (تعطيل بالواجهة + رفض بالخادم)؛ يمنع حتى النقل لسلة المحذوفات لا الحذف النهائي فقط.

---

## 10) البحث والفلترة

- **بحث محلي بكل قسم**: العملاء/الكفلاء/الدائنون بالاسم أو الهاتف؛ الديون/ديوني برقم الوصل أو اسم الطرف أو المبلغ.
- **منسدلة فلترة بجانب البحث**: العملاء (الكل/مفضلة)؛ الديون وديوني (الحالة، نوع السداد، العملة). تُطبَّق بمنطق **"و" (تقاطع)** مع بعضها ومع نص البحث — كل فلتر إضافي يضيّق النتائج.
- **بحث عام موحّد (Global Search)** بأعلى الشاشة: يطابق العملاء والكفلاء والديون معاً، نتائج مُجمَّعة بفئات، الضغط يفتح البطاقة مباشرة من أي صفحة.
- كل البحث/الفلترة من الخادم (debounced)، لا تحميل بيانات كاملة بالمتصفح.

---

## 11) إعدادات تطبيق المستخدم — ستة تبويبات

### 1. إدارة الحساب
- اسم المكتب، الشعار (رفع/ضغط)، اسم المستخدم وهاتفه (**للعرض فقط** — تغييره يتطلب تسجيل خروج ودخول برقم جديد كحساب منفصل)، تغيير كلمة المرور (بتأكيد صريح)، زر تسجيل الخروج.

### 2. عام
- اللغة، المظهر (فاتح/قاتم)، عدد الصفوف بكل صفحة، مدة التنبيه قبل السداد (`due_reminder_days`)، فترة السماح بعد التأخر (`overdue_grace_days`)، أيام إضافية لتصنيف "متلكئ" (`chronic_overdue_extra_days`).

### 3. الإيصالات
- رقم بداية وصل الدين ورقم بداية وصل التسديد — **كل منهما يُحدَّد مرة واحدة فقط عند الإعداد الأولي، ثم يُقفَل نهائياً** (لا تعديل لاحقاً، يمنع أي تعارض بالترقيم).

### 4. الاشتراكات والعروض
- عرض الباقة الحالية وتاريخ الانتهاء.
- **إدخال كود تفعيل**: يُتحقَّق أنه غير منتهٍ، غير مستخدَم، **ومخصَّص لهاتف هذا الحساب تحديداً** (`assigned_tenant_id`) — كود مخصَّص لحساب آخر يُرفَض دائماً.
- **طلب خطة**: الضغط على أي باقة معروضة يُنشئ طلباً (`plan_requests`) يظهر عند مدير المشروع للموافقة (تُفعِّل الخطة فوراً على نفس سجل `tenants`) أو الرفض (مع سبب). متاح حتى أثناء التجربة المجانية.

### 5. النسخ الاحتياطي (خاص ببيانات هذا المشترك فقط)
- نسخة يدوية أو تلقائية (يومي/أسبوعي/شهري/بدون).
- استرداد بتأكيد صريح قوي + نسخة أمان تلقائية قبل التنفيذ.
- **تحصين إضافي**: ملف موقَّع بالخادم (HMAC) لا خام — يُرفَض أي ملف لا يطابق توقيعه أو `tenant_id` الحساب الحالي؛ قائمة مركزية واحدة للجداول المرتبطة بـ`tenant_id` يستخدمها التصدير والاستيراد معاً؛ إعادة تأكيد كلمة المرور قبل الاسترداد (step-up).

### 6. حول (قراءة وتواصل فقط، محتوى موحّد يديره مدير المشروع)
- شرح التطبيق، رقم واتساب تفاعلي، اسم الشركة المطوّرة.
- **البريد الإلكتروني**: نموذج مراسلة داخل التطبيق (لا `mailto:`) — بريد الشركة ثابت، المستخدم يكتب بريده ونص الرسالة، الإرسال الفعلي من الخادم بـ`Reply-To` بريد المستخدم، مع `throttle` ضد السبام.

---

## 12) لوحة تحكم مدير المشروع

نفس مبدأ التخطيط والتصميم بالضبط (هيدر، قائمة جانبية/فوتر، جداول ثابتة، حفظ التصفّح، بحث/فلترة من الخادم) — فقط الأقسام تختلف:

### 0. الرئيسية
بطاقات مؤشرات (بلا رسوم بيانية، لا مؤشرات مالية بهذه المرحلة): إجمالي المشتركين، عدد كل حالة (نشط/تجربة/منتهي/معطّل/ملغى)، المقتربون من انتهاء التجربة/الاشتراك، طلبات خطط معلّقة، أكواد تفعيل غير مستخدمة — كل بطاقة تنتقل لقسمها بفلتر جاهز.

### 1. المشتركون (`tenants`)
- بحث (اسم/هاتف) + فلتر الحالة + فلتر النوع (أونلاين/أوفلاين).
- أعمدة بسيطة: الاسم، الهاتف، الحالة (بلون)، زر تفعيل/تعطيل مباشر.
- **حذف مشترك ممكن فقط بحالة "ملغى"** — حالة خامسة مستقلة عن "معطّل" (تعطيل=مؤقت وقابل لإعادة التفعيل؛ إلغاء=إنهاء متعمَّد يفتح إمكانية الحذف الناعم).
- بطاقة المشترك التفصيلية: بيانات الحساب، الباقة وتواريخ الانتهاء، عدد العملاء/الأجهزة مقابل حدود الباقة، سجل حركاته، إجراءات (تفعيل/تعطيل/إلغاء، توليد كود مباشر، تسجيل خروج من كل أجهزته [دعم فني]، تعديل استثنائي يدوي لتاريخ الاشتراك [يُسجَّل بالحركات]).

### 2. الباقات (`plans`)
جدول بسيط: الاسم، السعر، المدة، حد العملاء، حد الأجهزة. لا حذف لباقة مرتبط بها مشتركون؛ باقة التجربة المجانية غير قابلة للحذف إطلاقاً.

### 3. أكواد التفعيل (`activation_codes`)
- توليد كود: يختار مدير المشروع **المشترك المستهدف** + الباقة (تُغذَّى تلقائياً من قسم الباقات) + تاريخ الانتهاء.
- **كل كود مخصَّص لمشترك واحد ولباقة واحدة إلى الأبد** — لا يصلح لغيرهما.
- بحث/فلترة بالحالة (غير مستخدم/مستخدم/منتهي/ملغى)، زر نسخ سريع، إلغاء كود غير مستخدَم.

### 4. طلبات الخطط (`plan_requests`)
- بحث/فلترة بالحالة. عند فتح طلب: بيانات الطالب (هاتفه، باقته الحالية وتاريخ انتهائها، عدد عملائه مقابل حده) والباقة المطلوبة للمقارنة.
- موافقة (تحديث فوري لنفس سجل `tenants` — يظهر مباشرة بقسم المشتركين) أو رفض (بسبب مختصر). قرار نهائي لكل طلب.

### 5. حول
نموذج واحد (لا جدول) يحدّث `platform_settings`: شرح التطبيق، واتساب، بريد التواصل، اسم الشركة — يظهر فوراً لكل المشتركين دفعة واحدة.

### 6. سجل الحركات
كل إجراء إداري مؤثِّر (تفعيل/تعطيل/إلغاء، أكواد، طلبات خطط، باقات، محتوى "حول"، أدوات دعم فني) — بحث باسم المشترك المتأثر + فلتر بنوع الإجراء.

### 7. الإعدادات (تبويبان فقط)
- **إدارة الحساب**: بيانات دخول مدير المشروع، كلمة المرور، تسجيل الخروج.
- **عام**: شعار المنصة، مدة التجربة المجانية الافتراضية (`trial_days`)، اللغة، المظهر، عدد الصفوف بالجداول، **البلد** (كود الاتصال الدولي المركزي المستخدم بكل روابط واتساب بالنظام).

---

## 13) النسخة الأوفلاين

**الفكرة الجوهرية**: "أوفلاين" يعني مكان تخزين بيانات العمل فقط — لا استقلالية بالترخيص. الاشتراك شهري/سنوي متكرر بالضبط كالأونلاين، مُدار مركزياً بنفس النظام.

### التقنية
- **NativePHP**: نفس تطبيق Laravel+Inertia+Vue الحالي، يعمل كتطبيق أصلي على كمبيوتر وهاتف معاً (كل تثبيت مستقل).
- **قاعدة بيانات SQLite محلية** بدل MySQL المركزية.

### الترخيص المركزي
- التركيب يُسجَّل كسجل عادي بجدول `tenants` نفسه (`type=offline`) — نفس `plans`، `activation_codes`، `plan_requests`، `trial_ends_at`/`subscription_ends_at` بلا جدول منفصل. يظهر بقسم "المشتركون" بلوحة مدير المشروع (بفلتر النوع).
- **تفعيل أول إجباري بالاتصال** (كود تفعيل يُتحقَّق مركزياً).
- **تحقق دوري أسبوعي**: يعمل أوفلاين حراً بين كل تحقق، لكن يجب نجاح تحقق واحد كل أسبوع. عند الفشل (لا اتصال أو انتهاء اشتراك)، **يتوقف الإدخال الجديد فقط** (التصفّح يبقى متاحاً) لحين إعادة الاتصال. تنبيه مبكر من اليوم الخامس بدل قطع مفاجئ باليوم السابع.
- **تحصين ضد تلاعب ساعة الجهاز**: توقيت آخر تحقق ناجح يُحفَظ **موقّعاً (HMAC)** من الخادم لا من ساعة الجهاز الخام — أي تراجع بالساعة المحلية عن هذا التوقيت المحفوظ يُبطل التحقق فوراً.
- **حد الأجهزة مُفعَّل فعلياً**: كل تحقق دوري ناجح يسجّل بصمة الجهاز بنفس جدول `devices` — فيُفرَض `max_devices` تقنياً هنا أيضاً.
- التجديد (شهري/سنوي) بنفس آلية كود التفعيل/طلب الخطة، ويتطلب نفس الاتصال.

### الفرق الحقيقي الوحيد عن الأونلاين
بيانات العمل فقط (عملاء، ديون، أقساط، دفعات، دائنين، صور مستمسكات) تبقى محلياً بالكامل بقاعدة SQLite — **لا تُرفَع ولا تُزامَن مع الخادم إطلاقاً**. اتصال الخادم غرضه الوحيد التحقق من الاشتراك، لا نقل البيانات. الخادم المركزي لا يعرف عدد عملاء/ديون أي تركيب أوفلاين.

### لا مزامنة بين جهازين
المستخدم يختار جهازاً واحداً (هاتف أو حاسوب) — لا مزامنة بيانات بين تثبيتين مختلفين، كل تثبيت مستقل بقاعدته الخاصة.

### ما يبقى مطابقاً تماماً للأونلاين
كل الأقسام والقواعد التجارية (حاسبة الأقساط، قاعدة العملات، قيود الحذف، الوصولات، رقم الهاتف التفاعلي، تبويب "حول"، قفل التطبيق بالبصمة — أسهل تنفيذاً هنا فعلياً لأن NativePHP يوفّر وصولاً مباشراً لواجهات الجهاز الأصلية).

### النسخ الاحتياطي (مبسَّط جذرياً)
بما أن القاعدة ملف SQLite واحد مستقل، النسخ الاحتياطي هو ببساطة **نسخ الملف نفسه**، والاسترداد استبداله بنسخة سابقة — لا فلترة، لا خطر تسرّب بيانات مستخدم آخر (بعكس تعقيد الأونلاين على قاعدة بيانات مشتركة).

---

*آخر تحديث: 2026-08-23. للتفاصيل الحيّة والمناقشات المرتبطة بكل قرار، راجع محادثة التخطيط الأصلية.*
