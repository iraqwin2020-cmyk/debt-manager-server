# خطة مراحل إطلاق نظام إدارة الديون

تقسيم عملي لما وثِّق بـ[ARCHITECTURE.md](ARCHITECTURE.md) على ثلاث مراحل، بدل بناء كل شيء دفعة واحدة. كل ميزة موثّقة بالتفصيل بملف الهيكلية؛ هذا الملف يحدد فقط **متى** تُبنى.

---

## المرحلة الأولى — v1 (الإصدار القابل للإطلاق فعلياً)

النسخة الأونلاين فقط. لا أوفلاين إطلاقاً بهذه المرحلة.

### تطبيق المستخدم
- [x] تسجيل دخول ذاتي (هاتف + كلمة مرور) + تجربة مجانية تلقائية — **مُختبَر بالكامل بالمتصفح**
- [x] الصفحة الرئيسية — بطاقات مؤشرات بسيطة بلا رسوم بيانية — **مُختبَرة**، تعكس بيانات حقيقية (ديون له/عليه بكل عملة، عدد عملاء)
- [x] قسم العملاء (إضافة/تعديل/بحث/فلترة/سلة محذوفات) — **مُختبَر بالكامل**: إضافة، قائمة، بطاقة تفصيلية، حذف مع قيد الدين القائم
- [x] قسم الكفلاء — **مُختبَر بالكامل**: إضافة (تلقائية من نموذج الدين)، قائمة، بطاقة تفصيلية، **قيد الحذف مُختبَر فعلياً ونجح** (رفض حذف كفيل يتكفّل بعميل عليه دين)
- [x] قسم الديون: نموذج الدين الموحّد، نوعا السداد، عملتان بقاعدتي التقريب — **مُختبَر بالكامل** بما فيه حاسبة الأقساط (تحقّق مباشر من قاعدة البيانات: 1,000,000 ÷ 3 = 333,000+333,000+334,000 بالضبط)
- [x] فلترة "متأخر" ضمن قسم الديون (بدون قسم منفصل "المتلكئة" هذه المرحلة، كما هو مخطَّط)
- [x] قسم ديوني — **مُختبَر بالكامل**: إنشاء دائن جديد ودين عليه، ينعكس بالصفحة الرئيسية
- [x] قسم المفضلة — رابط جانبي + تبديل النجمة **مُختبَران**
- [x] بطاقة الشخص التفصيلية الكاملة (عميل وكفيل) — **مُبنية ومُختبَرة**: بيانات، ديون/عملاء مرتبطون، تعديل، حذف، دين جديد، مشاركة واتساب، طباعة كشف حساب
- [x] قيود الحذف — **مُختبَرة فعلياً وتعمل** (حظرت حذف كفيل بدين قائم)
- [x] رقم الهاتف التفاعلي — **مُختبَر ومعروض** بكل مكان
- [x] وصل الدين ووصل التسديد وكشف الحساب القابلون للطباعة — **مُبنيّون كصفحات Blade ومُختبَرون**: ترقيم تسلسلي صحيح، جدول أقساط، توقيع، BiDi
- [x] الإعدادات: إدارة الحساب، عام، الإيصالات، الاشتراك (كود تفعيل) — **مُختبَرة بالكامل**
- [x] حد جهاز واحد متزامن — **مُختبَر ضمنياً**: تحرير الجهاز عند تسجيل الخروج تأكَّد فعلياً

### لوحة مدير المشروع
- [x] قسم المشتركون (بحث/فلترة/تفعيل/تعطيل/إلغاء/حذف) — **مُختبَر بالكامل** بما فيه بطاقة تفصيلية وسجل حركات لكل مشترك
- [x] قسم الباقات (إضافة/تعديل/حذف بقيد الحماية) — **مُختبَر**، أُنشئت باقة جديدة فعلياً
- [x] قسم أكواد التفعيل (توليد/نسخ/إلغاء) — **مُختبَر**، تولّد كود واستُخدم لتفعيل مشترك فعلياً
- [x] سجل الحركات — يعمل تلقائياً بالخلفية (`ActivityLog::record`)، معروض بصفحة كل مشترك
- [x] الإعدادات (إدارة الحساب + عام) — تسجيل دخول/خروج مدير المشروع **مُختبَر**

### خارج نطاق v1 صراحة (لم يُبنَ، بحسب الخطة)
تبويب "حول" الكامل، طلبات الخطط (`plan_requests`)، النسخ الاحتياطي الذاتي بواجهة، مركز الإشعارات، قفل التطبيق بالبصمة، قسم "المتلكئة" المنفصل، تقرير الفترة الزمنية الشامل، مشاركة الوصل كصورة (Web Share API).

---

## المرحلة الثانية — بعد الإطلاق، حسب ردود فعل مستخدمين حقيقيين

- [ ] قسم "المتلكئة" (فوق "المتأخرة" الموجودة أصلاً)
- [ ] مركز الإشعارات (أيقونة جرس + جدول `notifications`)
- [ ] قفل التطبيق ببصمة/Face ID/رقم سري
- [ ] طلبات الخطط (`plan_requests`) — تكملة اختيارية لأكواد التفعيل
- [ ] تبويب "حول" الكامل (بما فيه نموذج المراسلة بالبريد)
- [ ] تقرير الفترة الزمنية الشامل
- [ ] مشاركة الوصل كصورة فعلية (Web Share API)
- [ ] تنبيه Push استباقي لانتهاء الاشتراك
- [ ] النسخ الاحتياطي الذاتي بواجهة كاملة لكل مشترك

---

## المرحلة الثالثة — توسّع لاحق

- [ ] النسخة الأوفلاين بالكامل (NativePHP، كمبيوتر وهاتف) — تُبنى بعد استقرار الأونلاين وتأكد الطلب الفعلي عليها
- [ ] دعم أجهزة متعددة/باقات أعلى (`max_devices > 1`)
- [ ] أي ميزات إضافية حسب طلب مستخدمين فعليين بعد الإطلاق

---

## قبل الإطلاق الفعلي (بقي بعد اكتمال v1 وظيفياً)

- [ ] Wireframes/مراجعة تصميم بصري احترافية (البناء الحالي يتبع الهوية اللونية لكن لم يُراجَع بصرياً بعناية كتصميم نهائي)
- [ ] خطة اختبار آلي (PHPUnit/Pest) — كل المنطق حالياً **تم التحقق يدوياً بالمتصفح بنجاح**، لكن لا اختبارات آلية محفوظة تمنع الانحدار مستقبلاً
- [ ] مراجعة أمنية عامة قبل النشر الفعلي (rate limiting شامل، مراجعة صلاحيات الملفات المرفوعة)
- [ ] ضبط بيئة الإنتاج الفعلية (cPanel، `.env` حقيقي، `php artisan migrate --force`، seeding الباقة الافتراضية وأول مدير مشروع)

---

*مرجع التفاصيل الكاملة لكل ميزة: [ARCHITECTURE.md](ARCHITECTURE.md).*
